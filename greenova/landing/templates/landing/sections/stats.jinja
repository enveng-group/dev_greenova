<section class="py-5 bg-success bg-opacity-10" aria-labelledby="stats-heading">
  <div class="container">
    <header class="mb-5 text-center">
      <h2 id="stats-heading" class="h1 fw-bold text-success mb-2">Key Performance Metrics</h2>
      <p class="lead text-secondary">Our impact in numbers: real-time compliance and industry leadership</p>
    </header>
    <div class="row g-4">
      <div class="col-12 col-md-6 col-lg-3">
        <div class="card h-100 border-0 shadow-sm text-center" _="on intersect if not window.matchMedia('(prefers-reduced-motion: reduce)').matches add .animate-fade-in to me then remove @intersect from me">
          <!-- Animation rationale: Fade-in on scroll using django-hyperscript. Accessible. -->
          <div class="card-body">
            <div class="mb-2">
              <img src="/static/core/img/icons/satisfaction.svg" alt="Compliance Rate icon" width="40" height="40" loading="lazy" />
            </div>
            <span class="display-4 fw-bold text-success">93%</span>
            <div class="text-secondary">Compliance Rate</div>
          </div>
        </div>
      </div>
      <div class="col-12 col-md-6 col-lg-3">
        <div class="card h-100 border-0 shadow-sm text-center" _="on intersect if not window.matchMedia('(prefers-reduced-motion: reduce)').matches add .animate-fade-in to me then remove @intersect from me">
          <!-- Animation rationale: Fade-in on scroll using django-hyperscript. Accessible. -->
          <div class="card-body">
            <div class="mb-2">
              <img src="/static/core/img/icons/obligations.svg" alt="Obligations Tracked icon" width="40" height="40" loading="lazy" />
            </div>
            <span class="display-4 fw-bold text-success">16,500+</span>
            <div class="text-secondary">Obligations Tracked</div>
          </div>
        </div>
      </div>
      <div class="col-12 col-md-6 col-lg-3">
        <div class="card h-100 border-0 shadow-sm text-center" _="on intersect if not window.matchMedia('(prefers-reduced-motion: reduce)').matches add .animate-fade-in to me then remove @intersect from me">
          <!-- Animation rationale: Fade-in on scroll using django-hyperscript. Accessible. -->
          <div class="card-body">
            <div class="mb-2">
              <img src="/static/core/img/icons/partners.svg" alt="Industry Partners icon" width="40" height="40" loading="lazy" />
            </div>
            <span class="display-4 fw-bold text-success">42</span>
            <div class="text-secondary">Industry Partners</div>
          </div>
        </div>
      </div>
      <div class="col-12 col-md-6 col-lg-3">
        <div class="card h-100 border-0 shadow-sm text-center" _="on intersect if not window.matchMedia('(prefers-reduced-motion: reduce)').matches add .animate-fade-in to me then remove @intersect from me">
          <!-- Animation rationale: Fade-in on scroll using django-hyperscript. Accessible. -->
          <div class="card-body">
            <div class="mb-2">
              <img src="/static/core/img/icons/insights.svg" alt="Client Satisfaction icon" width="40" height="40" loading="lazy" />
            </div>
            <span class="display-4 fw-bold text-success">98%</span>
            <div class="text-secondary">Client Satisfaction</div>
          </div>
        </div>
      </div>
    </div>
    <div class="mt-4 text-center text-secondary">
      Helping environmental professionals achieve compliance excellence since 2023
    </div>
  </div>
</section>

<style>
  @media (prefers-reduced-motion: no-preference) {
    .animate-fade-in {
      opacity: 0;
      animation: fadeIn 1s cubic-bezier(.4,0,.2,1) forwards;
    }
    @keyframes fadeIn {
      to { opacity: 1; }
    }
  }
</style>
