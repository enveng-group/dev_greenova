"""
Django settings for greenova project.

Generated by 'django-admin startproject' using Django 5.1.5.
"""
import mimetypes
from pathlib import Path
import os
import logging  # noqa: F401 - Used in logging configuration

# Validate required environment variables
required_env_vars = [
    "DJANGO_SECRET_KEY",
    "DJANGO_ALLOWED_HOSTS",
    "DJANGO_TIME_ZONE",
    "GREENOVA_ENVIRONMENT",
    "GREENOVA_VERSION",
]

missing_vars = [var for var in required_env_vars if var not in os.environ]
if missing_vars:
    raise Exception(f"Missing required environment variables: {', '.join(missing_vars)}")

# Build paths inside the project like this: BASE_DIR / 'subdir'.
BASE_DIR = Path(__file__).resolve().parent.parent

# Create logs directory if it doesn't exist
LOGS_DIR = BASE_DIR / "logs"
os.makedirs(LOGS_DIR, exist_ok=True)

# Quick-start development settings - unsuitable for production
# See https://docs.djangoproject.com/en/5.1/howto/deployment/checklist/

# SECURITY WARNING: keep the secret key used in production secret!
SECRET_KEY = os.environ["DJANGO_SECRET_KEY"]

# SECURITY WARNING: don't run with debug turned on in production!
DEBUG = os.environ.get('DJANGO_DEBUG', 'True') == 'True'

mimetypes.add_type("text/css", ".css", True)
mimetypes.add_type("application/javascript", ".js", True)
mimetypes.add_type("text/html", ".html", True)
mimetypes.add_type("image/svg+xml", ".svg", True)

ALLOWED_HOSTS = os.environ.get("DJANGO_ALLOWED_HOSTS", "localhost,127.0.0.1").split(",")

# Security Settings
# CSRF_TRUSTED_ORIGINS = [
#     "https://app.enssol.com.au",
#     "http://localhost:8000",
# ]

# HTTPS/SSL Settings
# SECURE_PROXY_SSL_HEADER = ("HTTP_X_FORWARDED_PROTO", "https")
# SECURE_SSL_REDIRECT = os.environ.get("DJANGO_SECURE_SSL_REDIRECT", "False") == "True"
# SESSION_COOKIE_SECURE = os.environ.get("DJANGO_SESSION_COOKIE_SECURE", "False") == "True"
# CSRF_COOKIE_SECURE = os.environ.get("DJANGO_CSRF_COOKIE_SECURE", "False") == "True"

# Environment detection
ENVIRONMENT = os.environ.get("GREENOVA_ENVIRONMENT", "development")
VERSION = os.environ.get("GREENOVA_VERSION", "1.0.0")

# Environment-specific settings
if ENVIRONMENT == "production":
#     SECURE_SSL_REDIRECT = True
#     SESSION_COOKIE_SECURE = True
#     CSRF_COOKIE_SECURE = True
#     SECURE_HSTS_SECONDS = 31536000
#     SECURE_HSTS_INCLUDE_SUBDOMAINS = True
#     SECURE_HSTS_PRELOAD = True
    pass

# Application definition

INSTALLED_APPS = [
    "django.contrib.admin",
    "django.contrib.auth",
    "django.contrib.contenttypes",
    "django.contrib.sessions",
    "django.contrib.messages",
    # 'whitenoise.runserver_nostatic',  # Must be above django.contrib.staticfiles
    'django.contrib.staticfiles',
    # Third party apps
    'chartjs',  # Change from 'django_chartjs' to 'chartjs'
    # 'compressor',
    'django_htmx',
    # Local apps
    "landing",
    "login",
    "dashboard",
    "users",
    "audit",
    "obligations",
    # "whitenoise"
]

# Add after INSTALLED_APPS

# Authentication settings
LOGIN_URL = 'login'
LOGIN_REDIRECT_URL = 'dashboard'
LOGOUT_REDIRECT_URL = 'index'

MIDDLEWARE = [
    'django.middleware.security.SecurityMiddleware',
    # 'whitenoise.middleware.WhiteNoiseMiddleware',  # Add this line
    "django.contrib.sessions.middleware.SessionMiddleware",
    "django.middleware.common.CommonMiddleware",
    "django.middleware.csrf.CsrfViewMiddleware",
    "django.contrib.auth.middleware.AuthenticationMiddleware",
    "django.contrib.messages.middleware.MessageMiddleware",
    "django.middleware.clickjacking.XFrameOptionsMiddleware",
    "django_htmx.middleware.HtmxMiddleware",
    "audit.middleware.AuditMiddleware",
]

# CORS Settings
CORS_ALLOWED_ORIGINS = [
    "https://app.enssol.com.au",
    "http://localhost:8000",
]
CORS_ALLOW_CREDENTIALS = True

ROOT_URLCONF = "greenova.urls"

TEMPLATES = [
    {
        "BACKEND": "django.template.backends.django.DjangoTemplates",
        "DIRS": [
            BASE_DIR / "templates",
            BASE_DIR / "templates/registration",  # Add this line
        ],
        "APP_DIRS": True,
        "OPTIONS": {
            "context_processors": [
                "django.template.context_processors.debug",
                "django.template.context_processors.request",
                "django.contrib.auth.context_processors.auth",
                "django.contrib.messages.context_processors.messages",
            ],
        },
    },
]

# ASGI_APPLICATION = "greenova.asgi.application"
# WSGI_APPLICATION = "greenova.wsgi.application"


# Database
# https://docs.djangoproject.com/en/5.1/ref/settings/#databases

DATABASES = {
    "default": {
        "ENGINE": os.environ.get("DATABASE_ENGINE", "django.db.backends.sqlite3"),
        "NAME": BASE_DIR / os.environ.get("DATABASE_NAME", "db.sqlite3"),
    }
}


# Password validation
# https://docs.djangoproject.com/en/5.1/ref/settings/#auth-password-validators

AUTH_PASSWORD_VALIDATORS = [
    {
        "NAME": "django.contrib.auth.password_validation.UserAttributeSimilarityValidator",
    },
    {
        "NAME": "django.contrib.auth.password_validation.MinimumLengthValidator",
    },
    {
        "NAME": "django.contrib.auth.password_validation.CommonPasswordValidator",
    },
    {
        "NAME": "django.contrib.auth.password_validation.NumericPasswordValidator",
    },
]


# Internationalization
# https://docs.djangoproject.com/en/5.1/topics/i18n/

LANGUAGE_CODE = "en-au"

TIME_ZONE = "Australia/Perth"

USE_I18N = True

USE_TZ = True

DEFAULT_CHARSET = "utf-8"

# Static files (CSS, JavaScript, Images)
STATIC_URL = '/static/'
STATIC_ROOT = BASE_DIR / 'staticfiles'
STATICFILES_DIRS = [
    BASE_DIR / 'static',
]

STATICFILES_FINDERS = [
    'django.contrib.staticfiles.finders.FileSystemFinder',
    'django.contrib.staticfiles.finders.AppDirectoriesFinder',
    # 'compressor.finders.CompressorFinder',
]

# Django Compressor settings
# COMPRESS_ENABLED = True
# COMPRESS_ROOT = STATIC_ROOT

# Whitenoise configuration
# STATICFILES_STORAGE = 'whitenoise.storage.CompressedManifestStaticFilesStorage'

# Media files
MEDIA_URL = "/media/"
MEDIA_ROOT = BASE_DIR / "media"

# Compression settings
# COMPRESS_ENABLED = True
# COMPRESS_CSS_FILTERS = ['compressor.filters.css_default.CssAbsoluteFilter']
# COMPRESS_JS_FILTERS = ['compressor.filters.jsmin.JSMinFilter']

# WhiteNoise config
# WHITENOISE_USE_FINDERS = True
# WHITENOISE_MANIFEST_STRICT = False
# WHITENOISE_COMPRESS = True
# WHITENOISE_ALLOW_ALL_ORIGINS = True

# Default primary key field type
# https://docs.djangoproject.com/en/5.1/ref/settings/#default-auto-field

DEFAULT_AUTO_FIELD = "django.db.models.BigAutoField"

# Email settings
EMAIL_BACKEND = os.environ.get(
    "EMAIL_BACKEND", "django.core.mail.backends.console.EmailBackend"
)
EMAIL_HOST = os.environ.get("EMAIL_HOST", "localhost")
EMAIL_PORT = int(os.environ.get("EMAIL_PORT", 1025))

# Email settings for password reset
EMAIL_BACKEND = 'django.core.mail.backends.console.EmailBackend'
EMAIL_HOST = 'smtp.gmail.com'
EMAIL_PORT = 587
EMAIL_USE_TLS = True
EMAIL_HOST_USER = os.environ.get('EMAIL_HOST_USER')
EMAIL_HOST_PASSWORD = os.environ.get('EMAIL_HOST_PASSWORD')

LOGIN_REDIRECT_URL = "/"
LOGOUT_REDIRECT_URL = "/"

# Logging
LOGGING = {
    "version": 1,
    "disable_existing_loggers": False,
    "formatters": {
        "verbose": {
            "format": "{levelname} {asctime} {module} {message}",
            "style": "{",
        },
    },
    "handlers": {
        "file": {
            "level": "INFO",
            "class": "logging.handlers.RotatingFileHandler",
            "filename": LOGS_DIR / "audit.log",
            "maxBytes": 1024 * 1024 * 5,  # 5 MB
            "backupCount": 5,
            "formatter": "verbose",
        },
        "console": {
            "level": "INFO",
            "class": "logging.StreamHandler",
            "formatter": "verbose",
        },
    },
    "loggers": {
        "audit": {
            "handlers": ["file", "console"],
            "level": "INFO",
            "propagate": True,
        },
    },
}

# File upload settings
FILE_UPLOAD_MAX_MEMORY_SIZE = int(os.environ.get('OBLIGATION_FILE_UPLOAD_MAX_SIZE', 10485760))
ALLOWED_UPLOAD_EXTENSIONS = os.environ.get(
    'OBLIGATION_ALLOWED_FILE_TYPES',
    '.pdf,.doc,.docx,.xls,.xlsx,.png,.jpg'
).split(',')

# Cache settings
# CACHES = {
#     "default": {
#         "BACKEND": "django.core.cache.backends.locmem.LocMemCache",
#         "LOCATION": os.environ.get('CACHE_KEY_PREFIX', 'greenova'),
#         "TIMEOUT": int(os.environ.get('CACHE_TIMEOUT', 300)),
#     }
# }

# Custom app settings
OBLIGATIONS_SETTINGS = {
    'PER_PAGE': int(os.environ.get('OBLIGATIONS_PER_PAGE', 25)),
    'AUTO_REMINDER_DAYS': int(os.environ.get('OBLIGATION_AUTO_REMINDER_DAYS', 14)),
    'CRITICAL_THRESHOLD_DAYS': int(os.environ.get('OBLIGATION_CRITICAL_THRESHOLD_DAYS', 7)),
    'EXPORT_FORMATS': os.environ.get('OBLIGATION_EXPORT_FORMATS', 'csv,xlsx,pdf').split(','),
}

AUDIT_SETTINGS = {
    'RETENTION_PERIOD': int(os.environ.get('AUDIT_RETENTION_PERIOD', 365)),
    'BATCH_SIZE': int(os.environ.get('AUDIT_BATCH_SIZE', 1000)),
    'EXPORT_FORMAT': os.environ.get('AUDIT_EXPORT_FORMAT', 'csv'),
}

# Chart.js settings
CHARTJS_SETTINGS = {
    'COLOR_SCHEME': os.environ.get('CHARTJS_COLOR_SCHEME', 'corporate'),
    'RESPONSIVE': os.environ.get('CHARTJS_RESPONSIVE', 'True') == 'True',
    'MAINTAIN_ASPECT_RATIO': os.environ.get('CHARTJS_MAINTAIN_ASPECT_RATIO', 'True') == 'True',
    'DEFAULT_HEIGHT': int(os.environ.get('CHARTJS_DEFAULT_HEIGHT', 300)),
}

# HTMX settings
HTMX_SETTINGS = {
    'TIMEOUT': int(os.environ.get('HTMX_TIMEOUT', 3000)),
    'INDICATOR_CLASS': os.environ.get('HTMX_INDICATOR_CLASS', 'htmx-indicator'),
    'REFRESH_INTERVAL': int(os.environ.get('HTMX_REFRESH_INTERVAL', 30000)),
}

if DEBUG:
    INTERNAL_IPS = ["127.0.0.1"]
    if os.environ.get('DEV_DEBUG_TOOLBAR', 'True') == 'True':
        INSTALLED_APPS.append('debug_toolbar')
        MIDDLEWARE.insert(0, 'debug_toolbar.middleware.DebugToolbarMiddleware')

    if os.environ.get('DEV_SQL_PRINT', 'True') == 'True':
        LOGGING['loggers']['django.db.backends'] = {
            'level': 'DEBUG',
            'handlers': ['console'],
        }
