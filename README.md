# Readme

# Run with Daphne
daphne -b 0.0.0.0 -p 8000 greenova.asgi:application

Run pip install -r requirements.txt to update dependencies
Install pre-commit hooks with pre-commit install
Run python setup.py develop for development installation